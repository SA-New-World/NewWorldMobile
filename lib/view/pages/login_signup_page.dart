import 'package:flutter/material.dart';

/// Création d'une page de connection/inscription
class SignInSignUpPage extends StatelessWidget {
  const SignInSignUpPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
